<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
<!--    android:roundIcon="@mipmap/ic_launcher_round"-->
<!--    android:icon="@mipmap/ic_launcher"-->
<!--    android:label="@string/app_name"-->
    <application
        android:name=".ui.MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/filter_image"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:roundIcon="@drawable/filter_image"
        android:theme="@style/Theme.AlgoVisualizer"
        tools:targetApi="31">
        <activity
            android:name=".ui.main_activity.MainActivity"
            android:exported="true"
            android:label="@string/installed_app_name"
            android:theme="@style/Theme.AlgoVisualizer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>